# Bitácora de Cambios - Proyecto Crea PDF Puppeteer

## [v1.9.0] - 09 de Marzo de 2026

### Experiencia de Usuario y Navegación
- **Gestión de Sesión en Interfaz Principal**:
  - Se implementó una **barra de estado de sesión** en la vista principal (`index.html`) que muestra el nombre del usuario autenticado.
  - Se añadió la funcionalidad de **Cerrar Sesión (Logout)** accesible en todo momento, permitiendo al usuario salir de forma segura y redirigiendo al login.
  - Nuevo endpoint `/api/me` para validación segura de la sesión activa desde el cliente.

### Mejoras en Registro y Login
- **Visibilidad de Contraseñas**:
  - Se integró una funcionalidad de **"Mostrar/Ocultar Contraseña"** en el formulario de registro.
  - Uso de iconos estándar (Bootstrap Icons `eye` / `eye-slash`) para una mejor experiencia visual e intuitiva.
  - Mantiene el foco en el campo de entrada para evitar que el formulario "se cierre" durante la interacción.

---

## [v1.8.0] - 06 de Marzo de 2026

### Seguridad y Control de Acceso (Dashboard)
- **Implementación de Login Institucional**:
  - Desarrollo de una nueva interfaz de inicio de sesión (`login.html`) con diseño animado basado en la identidad gráfica oficial de Conagua (Pantones 660 C, 534 C, 424 C y 1255 C).
  - Integración de `bcryptjs` para la encriptación segura de contraseñas.
  - Gestión de sesiones persistentes mediante `express-session` para controlar el acceso a la plataforma.

- **Arquitectura Multiusuario**:
  - Actualización del esquema de base de datos (`schema.prisma`) añadiendo la entidad `Usuario` y estableciendo relaciones con los reportes (`DatosFormato`).
  - Lógica de **Privacidad de Datos**: Cada usuario (a excepción de los administradores) visualiza únicamente su propio historial de reportes y borradores.
  - Implementación de roles de usuario en Backend:
    - **ADMIN**: Acceso global y supervisión de todos los registros.
    - **USER**: Acceso restringido a su propia información.

- **Herramientas de Administración**:
  - Creación del script `scripts/create-admin.ts` para la generación automática de cuentas administrativas iniciales.

---

## [v1.7.0] - 05 de Marzo de 2026

### Experiencia de Usuario (Solicitud de Reporte)
- **Flujo Inteligente de Reportes**: 
  - Al detectar el campo "Número de Reporte" vacío, el sistema despliega un **Modal Personalizado** (reemplazando alertas nativas).
  - Opción **"Solicitar Reporte"**: Abre el cliente de correo local (`mailto:`) con una plantilla predefinida y guarda los datos como *Borrador Pendiente*, sin generar el PDF inválido.
  - Opción **"Solo Generar"**: Crea el PDF marcando el registro como "PENDIENTE" en la base de datos.

### Backend y Base de Datos (Prisma)
- **Gestión de Estados**: 
  - Actualización del esquema `schema.prisma` para incluir el campo `estado` (COMPLETO/PENDIENTE) en el modelo `DatosFormato`.
  - Endpoint `/api/drafts` actualizado para filtrar y manejar solicitudes pendientes.
- **Independencia del Servidor de Correo**:
  - Se eliminó la dependencia de `nodemailer` en el backend.
  - Se implementó una solución `client-side` segura que utiliza el gestor de correo del sistema operativo del usuario.

### Interfaz de Usuario
- **Filtrado de Historial**:
  - Nuevo control (checkbox) en el modal de historial para mostrar u ocultar trámites pendientes.
  - Visualización clara del estado en la lista de borradores.

---

## [v1.6.1] - 03 de Marzo de 2026

### Despliegue y Automatización (DevOps)
- **Script de Despliegue Automatizado (`deploy.sh`)**:
  - Se creó un script en Bash para servidores Linux que gestiona el ciclo de vida de los contenedores:
    - Verifica dependencias (Docker/Compose).
    - Detiene, reconstruye y levanta servicios.
    - Espera la disponibilidad de la BD.
    - Ejecuta migraciones de Prisma de forma automática.
    - Limpia imágenes en desuso.
- **Arquitectura "Zero-Dependencies"**:
  - Se validó que el servidor de producción no requiere instalaciones locales de Node.js, PostgreSQL ni Chrome; todo se ejecuta dentro de los contenedores aislados.
- **Estrategia de Configuración**:
  - Eliminación de dependencia del archivo `.env` local para producción.
  - Configuración de variables de entorno (DB, contraseñas) directamente en `docker-compose.yml`.
  - Definición de "Lista Blanca" de archivos necesarios para subir al servidor (carpeta `deploy_crea_pdf`).

---

## [v1.6.0] - 02 de Marzo de 2026

### Infraestructura (Docker)
- **Containerización Completa**:
  - Se creó el archivo `Dockerfile` basado en la imagen `node:20-slim`.
  - Configuración optimizada para instalar Chrome y fuentes necesarias para Puppeteer en entornos Linux/Debian.
  - Generación automática del cliente de Prisma (`prisma generate`) durante la construcción de la imagen.
  - Copiado manual de recursos estáticos (`src/views`) a la carpeta de compilación `dist`.
- **Orquestación con Docker Compose**:
  - Se añadió `docker-compose.yml` para desplegar la aplicación junto con una base de datos PostgreSQL.
  - Configuración de volúmenes persistentes para la base de datos (`pgdata`) y para los PDFs generados (`./output_pdfs`).
  - Variables de entorno preconfiguradas para la conexión DB y rutas de ejecutables de Chrome.

### Documentación
- **Ignorados de Docker**:
  - Archivo `.dockerignore` configurado para excluir `node_modules`, archivos fuente innecesarios y documentación del contexto de construcción, reduciendo el tamaño de la imagen final.



## [v1.5.0] - 26 de Febrero de 2026
### Mejorado
- **Interfaz (UX/UI)**: 
  - Rediseño completo del botón "Cargar Historial" y del modal de selección, adoptando los colores institucionales (Verde y Dorado) y mejorando la jerarquía visual.
  - Se añadió funcionalidad para cerrar el modal haciendo clic fuera de él o usando la "X" superior.
- **Campo Justificación**: 
  - Automatización para que siempre aparezca como el **último campo** del formulario.
  - Diseño "Full Width" (ancho completo) para mejorar la experiencia de escritura en explicaciones largas.
- **Ordenamiento Alfabético**: 
  - Se implementó `localeCompare` en `catalogService.ts` para que todos los listados (Puestos, Áreas, etc.) se devuelvan ordenados alfabéticamente (A-Z) respetando la gramática española.

## [v1.4.0] - 25 de Febrero de 2026
### Agregado
- **Sistema de Historial y Borradores**: 
  - Se implementó un botón "📂 Cargar Borrador / Historial" en la interfaz principal.
  - Modal para visualizar y seleccionar borradores guardados anteriormente.
  - Funcionalidad `loadDraft` para rellenar automáticamente el formulario con datos de sesiones previas.
- **Persistencia de Edición**:
  - Al cargar un borrador, el sistema ahora actualiza el registro existente en lugar de duplicarlo al volver a generar el PDF (`currentDraftId`).
  - Feedback visual mediante "Snackbars" al cargar o actualizar borradores.

### Modificado
- **Frontend (`app.js`)**: Refactorización para manejar la carga asíncrona de datos en selectores dependientes (ej: Dirección, Área) al restaurar un borrador.
- **Backend**: Ajuste en los headers de respuesta para devolver el ID del borrador procesado.

## [v1.3.0] - 24 de Febrero de 2026
### Agregado
- **Nuevos Formatos (Honorarios)**: Se integraron archivos y lógica para el manejo de "Alta, Baja y Cambio de usuario Honorarios", utilizando plantillas específicas para este tipo de usuario.

### Modificado
- **Ajuste de Coordenadas**: Se refinaron las coordenadas CSS de los campos en las plantillas HTML para alinear el texto perfectamente con los formatos de fondo.
- **Limpieza de Código (MongoDB)**: Se eliminó el archivo `src/services/mongoClient.ts` y todas las referencias a MongoDB en el proyecto, consolidando el uso de archivos CSV locales como única fuente de datos para catálogos.

### Corregido
- **Configuración Prisma**: Se actualizó `prisma/schema.prisma` para cumplir con los requisitos de Prisma v7+, moviendo la configuración de la URL de conexión a `prisma.config.ts` y eliminando la propiedad `url` obsoleta en el bloque `datasource`.

## [v1.2.0] - 10 de Febrero de 2026
### Agregado
- **Validación de Fechas**: Se implementó lógica para el campo `INICIO_ACTIVIDADES` (y cualquier campo que contenga "fecha").
  - Se restringe la selección de fechas anteriores al día actual (`min={today}`).
  - Se detecta automáticamente por nombre de campo (`/fecha|inicio_actividades/i`).

## [v1.1.0] - 06 de Febrero de 2026
### Agregado
- **Justificación Manual**: Se añadió un checkbox "Escribir justificación manualmente".
  - Al activarlo, reemplaza el menú desplegable por un `textarea`.
  - El texto se alinea a la izquierda para mejor legibilidad.
- **Validación de Formularios**:
  - Se estandarizaron los mensajes de error HTML5 (`setCustomValidity`).
  - Mensaje unificado: "Completa este campo" para todos los inputs requeridos.

### Modificado
- **Diseño del Encabezado (Header)**:
  - Título "Generar Formato ABC" centrado.
  - Fecha actual mostrada en un contenedor tipo "badge" alineado a la derecha.
  - Diseño responsivo para pantallas pequeñas.
- **Lógica de Catálogos**:
  - Filtrado dinámico de justificaciones según el tipo de plantilla seleccionada (ALTA, BAJA, CAMBIO).
  - Backend: Clasificación automática de justificaciones leyendo el prefijo del texto en `catalogService.ts`.

## [v1.0.0] - 16 de Enero de 2026
### Inicial
- **Motor de Renderizado**: Cambio de estrategia para generación de PDFs.
  - Se eliminaron las tablas HTML con bordes dibujados.
  - Se implementó el uso de imágenes de fondo (`background-image`) extraídas de los formatos oficiales.
  - Ajuste de coordenadas CSS para superponer el texto en las posiciones exactas de la imagen.

---
*Generado automáticamente por GitHub Copilot*
